import 'package:flutter_test/flutter_test.dart';

// Isar testleri Faz 2'de SessionEntrySchema eklendikten sonra buraya gelecek.
// Isar.open() en az bir schema gerektiriyor — Faz 1'de henüz model yok.
void main() {
  test('placeholder — Isar testleri Faz 2 ile aktif olacak', () {
    expect(true, isTrue);
  });
}
